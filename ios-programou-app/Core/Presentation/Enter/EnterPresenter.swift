final class EnterPresenter {
    let title = EnterLocalize.title.text
    let description = EnterLocalize.description.text
    let nameLabel = EnterLocalize.nameTextFieldLabel.text
    let namePlaceholder = EnterLocalize.nameTextFieldPlaceholder.text
    let accountAccessButtonDescription = EnterLocalize.accessButtonDescription.text
    let accountAccessButtonTitle = EnterLocalize.accessButtonTitle.text
    let nextStepButtonTitle = EnterLocalize.nextStepButtonTitle.text
}
